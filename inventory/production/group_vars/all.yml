---
# Global settings
timezone: "Asia/Tashkent"
ntp_enabled: true
ntp_servers:
  - 0.pool.ntp.org
  - 1.pool.ntp.org

# Backup settings
backup_enabled: true
backup_path: "/backup"
backup_retention_days: 30

# Monitoring
snmp_enabled: false
snmp_community: "public"

# Logging
log_enabled: true
syslog_server: "192.168.1.100"
